Do these 5 tasks in my Multilingual Healthcare Assistant app, built modern, mobile-responsive, and professional throughout: 1) MIC (Voice to Text) — add a visible 🎤 mic button inside the chat input bar, plus a small language selector (Hausa ha-NG / English en-US / French fr-FR / Arabic ar-SA) next to it since Web Speech API can only listen in one language per session; on click, request mic permission and start webkitSpeechRecognition with the selected language, show a "Listening..." animation while active, auto-fill the input field with the transcribed text, and feature-detect browser support — if webkitSpeechRecognition is unavailable (e.g. Safari/iOS), hide the mic button and show a small "voice input not supported on this browser" note instead of failing silently; test on mobile Chrome Android. 2) EMERGENCY HOSPITAL FINDER — detect a broad set of emergency phrases per language (not just exact keywords — include variations like "can't breathe," "chest hurts," "bleeding a lot," and Hausa/French/Arabic equivalents), and when triggered, show a clearly styled "🚨 EMERGENCY — Find Help Near You" section with: a "Open Nearest Hospitals in Google Maps" button linking to https://www.google.com/maps/search/hospital+near+me/ (uses the user's live location via the browser, so it works nationwide and for users anywhere, not just Lagos), Nigeria's nationwide emergency numbers 112 (National Emergency – police/fire/medical) and 199 (Police, nationwide), and a disclaimer: "This is a supplementary alert, not a diagnosis — always call emergency services directly." 3) SAVE CHAT HISTORY / PDF REPORT — add a "Download Report as PDF" button at the top that generates a PDF (via jsPDF) of the full conversation with date, time, and a medical disclaimer footer; save chat history in localStorage so it survives reloads, with a visible note "Chat history is stored locally on your device only"; add a "Clear Chat" button that wipes both the UI and localStorage. 4) DOCTOR DASHBOARD — create a /dashboard route protected by a simple password gate (client-side is fine for this demo, but note in a comment that it is not production-grade security), showing: total chats today, total emergency cases detected, a list of recent chats pulled from localStorage, and a language-distribution breakdown (Hausa vs English vs French vs Arabic) rendered as simple CSS bar/pie charts — clean, professional layout. 5) HOMEPAGE COPY — replace the hero title with "MediLingua AI — Nigeria's First Multilingual Healthcare Assistant," the subtitle with "Breaking Language Barriers in Healthcare | Supports Hausa, English, French & Arabic | Voice, Image & Emergency Triage | Trusted for Rural & Urban Clinics," and add badges below it: "🎤 Voice-Enabled | 🖼️ Image Analysis | 🚨 Emergency Detection | 📄 PDF Reports." Test every feature end-to-end and confirm the deployment build is not broken before finishing.